[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server build"]

[start]
cmd = "NODE_ENV=production pnpm --filter @workspace/api-server start"
